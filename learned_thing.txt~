npm create vite@latest . --template react //directly creates in vite enviroment
need to have VITE_APP in the env variable to access the files

@supabase/supabase.js node module name

need to import variable name like meta.env


supabase = createClient(url, key)

supaBase.form('table_name').insert([{description}->table-columns]:lists).select()->returns the whole values;



