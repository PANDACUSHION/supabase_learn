const UserCard = ({user}) =>{
    return(
        <>
            <div className="usercard">
                <h1>{user.id}</h1>
                <h2>{user.created_at}</h2>
                <h3>{user.description}</h3>
            </div>
        </>
    )
}